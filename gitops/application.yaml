apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taskapp-gitops
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.comhttps/lovesmesegun/capstone-phoenix.git
    targetRevision: main
    path: manifests
  destination:
    server: https://default.svc
    namespace: taskapp
  # PERMANENT VISUAL FIX: Tells the engine to ignore bare-metal networking state variations
  ignoreDifferences:
  - group: networking.k8s.io
    kind: Ingress
    jsonPointers:
    - /status
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

