apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taskapp-gitops
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lovesmesegun/capstone-phoenix.git
    targetRevision: main
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: taskapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
