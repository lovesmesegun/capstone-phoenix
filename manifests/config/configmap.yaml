apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  POSTGRES_DB: "taskapp"
  POSTGRES_USER: "taskapp"
  DB_HOST: "postgres"
  DATABASE_HOST: "postgres"
  POSTGRES_HOST: "postgres"
